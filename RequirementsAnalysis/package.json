{
  "name": "requirement-agent-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "example": "npm run build && node dist/requirement-agent/example.js",
    "test": "npm run build && node dist/requirement-agent/requirementAgent.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0"
  }
}
