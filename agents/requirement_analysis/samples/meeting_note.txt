会议目标：整理任务看板的前端需求，支持团队跟踪任务进度。

核心诉求：
1. 用户可以看到任务列表，包含任务标题、负责人、截止时间和当前状态。
2. 用户可以将任务标记为完成，完成后状态在页面中应明确可见。
3. 当任务数据加载失败或状态更新失败时，页面需要提示用户并给出可理解文案。
4. 移动端场景下也要保持信息可读和操作便捷。

范围约束：
- 本期不做任务创建和删除。
- 本期不做复杂筛选和批量操作。
