name: FrontEnd Pages Deployment Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: echo "FrontEnd GitHub Pages deployment is intentionally disabled during repository migration."
